"use strict";
const myName = "lola";

if (myName === "Ana Redondo" || myName === "Daniela Salazar") {
  console.log(`Bienvenida, ${myName}`);
} else {
  console.log(
    "Lo siento pero el usuario que has introducido no está registrado"
  );
}
